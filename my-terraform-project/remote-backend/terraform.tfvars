# Optionally, you can override the default values

s3_bucket_name = "custom-terraform-state-bucket-123456" # it should be unique
dynamodb_table_name = "custom-terraform-state-locks-123456" # it should be unique